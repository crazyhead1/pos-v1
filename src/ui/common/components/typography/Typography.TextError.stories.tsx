import { TextError } from '.';

export default {
  component: TextError,
  title: 'atoms/Typography/TextError',
};

const Template = args => <TextError {...args} />;

export const Default = Template.bind({});
